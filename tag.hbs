{{!< default}}

<header class="tag-header">
    <h1 class="tag-title">{{tag.name}}</h1>
    {{#if tag.description}}
    <p class="tag-description">{{tag.description}}</p>
    {{/if}}
</header>

<div class="post-feed">
    {{#foreach posts}}
        {{> "list-post"}}
    {{/foreach}}
</div>

{{pagination}}
