<div class="cta-bar" aria-label="Campaign actions">
    <div class="cta-bar-inner">
        <a href="{{@custom.donate_url}}" class="btn btn-accent">Donate</a>
        <a href="{{@custom.volunteer_url}}" class="btn btn-secondary">Volunteer</a>
    </div>
</div>
